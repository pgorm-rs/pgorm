{
  "version": 1,
  "cases": [
    {
      "id": "select",
      "family": "CRUD values",
      "api": "Entity::find/filter",
      "method": "GET",
      "route": "/case/protected/select",
      "field": "input",
      "baseline": "alice",
      "alternate": "bob",
      "techniques": [
        "B",
        "E",
        "U",
        "S",
        "T",
        "Q"
      ],
      "controls": [
        "/case/control/select"
      ],
      "fixed": {
        "table": "fixture.items",
        "tenant_guard": 1,
        "sentinel_id": 3
      },
      "inapplicable": {
        "Q": {
          "reason": "Inline-query tests are REPLACE-only (where=3), so the vector substitutes the whole parameter value with no prefix or suffix to escape quoting, and the technique confirms only by grepping its own delimiters out of the response body. The control puts the input in a single-quoted literal in a SELECT WHERE predicate, from which the vector cannot reach an unquoted, reflected projection; pgorm binds values and double-quotes identifiers, so no faithful control anywhere in this suite exposes one.",
          "evidence": {
            "kind": "no-boundary",
            "payload": "inline_query.xml",
            "where": [
              3
            ],
            "clause": [
              1,
              2,
              3,
              8
            ],
            "boundary": "boundaries.xml holds exactly one boundary whose <where> includes 3 (level 1, clause 0, ptype 1) and it carries an empty <prefix> and empty <suffix>; no where=3 boundary exists at any level that closes a ' or a \", so REPLACE can only land on an already-unquoted value.",
            "contexts": "4"
          }
        }
      }
    },
    {
      "id": "insert",
      "family": "CRUD values",
      "api": "Insert::select_from",
      "method": "GET",
      "route": "/case/protected/insert",
      "field": "input",
      "baseline": "alice",
      "alternate": "bob",
      "techniques": [
        "B",
        "E",
        "U",
        "S",
        "T",
        "Q"
      ],
      "controls": [
        "/case/control/insert"
      ],
      "fixed": {
        "table": "fixture.items",
        "tenant_guard": 1,
        "sentinel_id": 3
      },
      "inapplicable": {
        "Q": {
          "reason": "Inline-query tests are REPLACE-only (where=3), so the vector substitutes the whole parameter value with no prefix or suffix to escape quoting, and the technique confirms only by grepping its own delimiters out of the response body. The control puts the input in a single-quoted literal in the WHERE of an INSERT ... SELECT, from which the vector cannot reach an unquoted, reflected projection; pgorm binds values and double-quotes identifiers, so no faithful control anywhere in this suite exposes one.",
          "evidence": {
            "kind": "no-boundary",
            "payload": "inline_query.xml",
            "where": [
              3
            ],
            "clause": [
              1,
              2,
              3,
              8
            ],
            "boundary": "boundaries.xml holds exactly one boundary whose <where> includes 3 (level 1, clause 0, ptype 1) and it carries an empty <prefix> and empty <suffix>; no where=3 boundary exists at any level that closes a ' or a \", so REPLACE can only land on an already-unquoted value.",
            "contexts": "4"
          }
        }
      }
    },
    {
      "id": "update-value",
      "family": "CRUD values",
      "api": "Update::many/col_expr",
      "method": "GET",
      "route": "/case/protected/update-value",
      "field": "input",
      "baseline": "alice",
      "alternate": "bob",
      "techniques": [
        "B",
        "E",
        "U",
        "S",
        "T",
        "Q"
      ],
      "controls": [
        "/case/control/update-value"
      ],
      "fixed": {
        "table": "fixture.items",
        "tenant_guard": 1,
        "sentinel_id": 3
      },
      "inapplicable": {
        "Q": {
          "reason": "Inline-query tests are REPLACE-only (where=3), so the vector substitutes the whole parameter value with no prefix or suffix to escape quoting, and the technique confirms only by grepping its own delimiters out of the response body. The control puts the input in a single-quoted literal in an UPDATE WHERE predicate, from which the vector cannot reach an unquoted, reflected projection; pgorm binds values and double-quotes identifiers, so no faithful control anywhere in this suite exposes one.",
          "evidence": {
            "kind": "no-boundary",
            "payload": "inline_query.xml",
            "where": [
              3
            ],
            "clause": [
              1,
              2,
              3,
              8
            ],
            "boundary": "boundaries.xml holds exactly one boundary whose <where> includes 3 (level 1, clause 0, ptype 1) and it carries an empty <prefix> and empty <suffix>; no where=3 boundary exists at any level that closes a ' or a \", so REPLACE can only land on an already-unquoted value.",
            "contexts": "4"
          }
        },
        "U": {
          "reason": "Every qualifying UNION test appends at where=1 and needs a SELECT set-operation slot to attach to. The control is an UPDATE statement whose injection point is a single-quoted literal in an UPDATE WHERE predicate; a UNION SELECT cannot attach to an UPDATE's WHERE, so making this reachable would mean making the control a SELECT, which stops it being an UPDATE control.",
          "evidence": {
            "kind": "no-attachable-position",
            "payload": "union_query.xml",
            "where": [
              1
            ],
            "clause": [
              1,
              2,
              3,
              4,
              5
            ],
            "boundary": "A level-3 ptype-2 `'` boundary closes the quote and the UNION payload supplies its own [GENERIC_SQL_COMMENT], but the enclosing INSERT/UPDATE/DELETE offers no set-operation position for the appended SELECT at any boundary level.",
            "contexts": "7a"
          }
        }
      }
    },
    {
      "id": "update-guard",
      "family": "CRUD values",
      "api": "Update::many/filter",
      "method": "GET",
      "route": "/case/protected/update-guard",
      "field": "input",
      "baseline": "alice",
      "alternate": "bob",
      "techniques": [
        "B",
        "E",
        "U",
        "S",
        "T",
        "Q"
      ],
      "controls": [
        "/case/control/update-guard"
      ],
      "fixed": {
        "table": "fixture.items",
        "tenant_guard": 1,
        "sentinel_id": 3
      },
      "inapplicable": {
        "Q": {
          "reason": "Inline-query tests are REPLACE-only (where=3), so the vector substitutes the whole parameter value with no prefix or suffix to escape quoting, and the technique confirms only by grepping its own delimiters out of the response body. The control puts the input in a single-quoted literal in an UPDATE WHERE predicate, from which the vector cannot reach an unquoted, reflected projection; pgorm binds values and double-quotes identifiers, so no faithful control anywhere in this suite exposes one.",
          "evidence": {
            "kind": "no-boundary",
            "payload": "inline_query.xml",
            "where": [
              3
            ],
            "clause": [
              1,
              2,
              3,
              8
            ],
            "boundary": "boundaries.xml holds exactly one boundary whose <where> includes 3 (level 1, clause 0, ptype 1) and it carries an empty <prefix> and empty <suffix>; no where=3 boundary exists at any level that closes a ' or a \", so REPLACE can only land on an already-unquoted value.",
            "contexts": "4"
          }
        },
        "U": {
          "reason": "Every qualifying UNION test appends at where=1 and needs a SELECT set-operation slot to attach to. The control is an UPDATE statement whose injection point is a single-quoted literal in an UPDATE WHERE predicate; RETURNING cannot be unioned onto through the WHERE, so making this reachable would mean making the control a SELECT, which stops it being an UPDATE control.",
          "evidence": {
            "kind": "no-attachable-position",
            "payload": "union_query.xml",
            "where": [
              1
            ],
            "clause": [
              1,
              2,
              3,
              4,
              5
            ],
            "boundary": "A level-3 ptype-2 `'` boundary closes the quote and the UNION payload supplies its own [GENERIC_SQL_COMMENT], but the enclosing INSERT/UPDATE/DELETE offers no set-operation position for the appended SELECT at any boundary level.",
            "contexts": "7a"
          }
        }
      }
    },
    {
      "id": "delete",
      "family": "CRUD values",
      "api": "Delete::many/filter",
      "method": "GET",
      "route": "/case/protected/delete",
      "field": "input",
      "baseline": "alice",
      "alternate": "bob",
      "techniques": [
        "B",
        "E",
        "U",
        "S",
        "T",
        "Q"
      ],
      "controls": [
        "/case/control/delete"
      ],
      "fixed": {
        "table": "fixture.items",
        "tenant_guard": 1,
        "sentinel_id": 3
      },
      "inapplicable": {
        "Q": {
          "reason": "Inline-query tests are REPLACE-only (where=3), so the vector substitutes the whole parameter value with no prefix or suffix to escape quoting, and the technique confirms only by grepping its own delimiters out of the response body. The control puts the input in a single-quoted literal in a DELETE WHERE predicate, from which the vector cannot reach an unquoted, reflected projection; pgorm binds values and double-quotes identifiers, so no faithful control anywhere in this suite exposes one.",
          "evidence": {
            "kind": "no-boundary",
            "payload": "inline_query.xml",
            "where": [
              3
            ],
            "clause": [
              1,
              2,
              3,
              8
            ],
            "boundary": "boundaries.xml holds exactly one boundary whose <where> includes 3 (level 1, clause 0, ptype 1) and it carries an empty <prefix> and empty <suffix>; no where=3 boundary exists at any level that closes a ' or a \", so REPLACE can only land on an already-unquoted value.",
            "contexts": "4"
          }
        },
        "U": {
          "reason": "Every qualifying UNION test appends at where=1 and needs a SELECT set-operation slot to attach to. The control is an DELETE statement whose injection point is a single-quoted literal in a DELETE WHERE predicate; RETURNING cannot be unioned onto through the WHERE, so making this reachable would mean making the control a SELECT, which stops it being an DELETE control.",
          "evidence": {
            "kind": "no-attachable-position",
            "payload": "union_query.xml",
            "where": [
              1
            ],
            "clause": [
              1,
              2,
              3,
              4,
              5
            ],
            "boundary": "A level-3 ptype-2 `'` boundary closes the quote and the UNION payload supplies its own [GENERIC_SQL_COMMENT], but the enclosing INSERT/UPDATE/DELETE offers no set-operation position for the appended SELECT at any boundary level.",
            "contexts": "7a"
          }
        }
      }
    },
    {
      "id": "tenant-or",
      "family": "Composed predicates",
      "api": "Condition::any + tenant guard",
      "method": "GET",
      "route": "/case/protected/tenant-or",
      "field": "input",
      "baseline": "alice",
      "alternate": "nobody",
      "techniques": [
        "B",
        "E",
        "U",
        "S",
        "T",
        "Q"
      ],
      "controls": [
        "/case/control/tenant-or"
      ],
      "fixed": {
        "table": "fixture.items",
        "tenant_guard": 1,
        "sentinel_id": 3
      },
      "inapplicable": {
        "Q": {
          "reason": "Inline-query tests are REPLACE-only (where=3), so the vector substitutes the whole parameter value with no prefix or suffix to escape quoting, and the technique confirms only by grepping its own delimiters out of the response body. The control puts the input in a single-quoted literal in a DELETE WHERE predicate, from which the vector cannot reach an unquoted, reflected projection; pgorm binds values and double-quotes identifiers, so no faithful control anywhere in this suite exposes one.",
          "evidence": {
            "kind": "no-boundary",
            "payload": "inline_query.xml",
            "where": [
              3
            ],
            "clause": [
              1,
              2,
              3,
              8
            ],
            "boundary": "boundaries.xml holds exactly one boundary whose <where> includes 3 (level 1, clause 0, ptype 1) and it carries an empty <prefix> and empty <suffix>; no where=3 boundary exists at any level that closes a ' or a \", so REPLACE can only land on an already-unquoted value.",
            "contexts": "4"
          }
        },
        "U": {
          "reason": "Every qualifying UNION test appends at where=1 and needs a SELECT set-operation slot to attach to. The control is an DELETE statement whose injection point is a single-quoted literal in a DELETE WHERE predicate; RETURNING cannot be unioned onto through the WHERE, so making this reachable would mean making the control a SELECT, which stops it being an DELETE control.",
          "evidence": {
            "kind": "no-attachable-position",
            "payload": "union_query.xml",
            "where": [
              1
            ],
            "clause": [
              1,
              2,
              3,
              4,
              5
            ],
            "boundary": "A level-3 ptype-2 `'` boundary closes the quote and the UNION payload supplies its own [GENERIC_SQL_COMMENT], but the enclosing INSERT/UPDATE/DELETE offers no set-operation position for the appended SELECT at any boundary level.",
            "contexts": "7a"
          }
        }
      }
    },
    {
      "id": "empty-in",
      "family": "Composed predicates",
      "api": "is_in(empty) + guard",
      "method": "GET",
      "route": "/case/protected/empty-in",
      "field": "input",
      "baseline": "alice",
      "alternate": "bob",
      "techniques": [
        "B",
        "E",
        "U",
        "S",
        "T",
        "Q"
      ],
      "controls": [
        "/case/control/empty-in"
      ],
      "fixed": {
        "table": "fixture.items",
        "tenant_guard": 1,
        "sentinel_id": 3
      },
      "inapplicable": {
        "Q": {
          "reason": "Inline-query tests are REPLACE-only (where=3), so the vector substitutes the whole parameter value with no prefix or suffix to escape quoting, and the technique confirms only by grepping its own delimiters out of the response body. The control puts the input in a single-quoted literal in a DELETE WHERE predicate, from which the vector cannot reach an unquoted, reflected projection; pgorm binds values and double-quotes identifiers, so no faithful control anywhere in this suite exposes one.",
          "evidence": {
            "kind": "no-boundary",
            "payload": "inline_query.xml",
            "where": [
              3
            ],
            "clause": [
              1,
              2,
              3,
              8
            ],
            "boundary": "boundaries.xml holds exactly one boundary whose <where> includes 3 (level 1, clause 0, ptype 1) and it carries an empty <prefix> and empty <suffix>; no where=3 boundary exists at any level that closes a ' or a \", so REPLACE can only land on an already-unquoted value.",
            "contexts": "4"
          }
        },
        "U": {
          "reason": "Every qualifying UNION test appends at where=1 and needs a SELECT set-operation slot to attach to. The control is an DELETE statement whose injection point is a single-quoted literal in a DELETE WHERE predicate; RETURNING cannot be unioned onto through the WHERE, so making this reachable would mean making the control a SELECT, which stops it being an DELETE control.",
          "evidence": {
            "kind": "no-attachable-position",
            "payload": "union_query.xml",
            "where": [
              1
            ],
            "clause": [
              1,
              2,
              3,
              4,
              5
            ],
            "boundary": "A level-3 ptype-2 `'` boundary closes the quote and the UNION payload supplies its own [GENERIC_SQL_COMMENT], but the enclosing INSERT/UPDATE/DELETE offers no set-operation position for the appended SELECT at any boundary level.",
            "contexts": "7a"
          }
        }
      }
    },
    {
      "id": "empty-or",
      "family": "Composed predicates",
      "api": "Condition::any(empty) + guard",
      "method": "GET",
      "route": "/case/protected/empty-or",
      "field": "input",
      "baseline": "alice",
      "alternate": "bob",
      "techniques": [
        "B",
        "E",
        "U",
        "S",
        "T",
        "Q"
      ],
      "controls": [
        "/case/control/empty-or"
      ],
      "fixed": {
        "table": "fixture.items",
        "tenant_guard": 1,
        "sentinel_id": 3
      },
      "inapplicable": {
        "Q": {
          "reason": "Inline-query tests are REPLACE-only (where=3), so the vector substitutes the whole parameter value with no prefix or suffix to escape quoting, and the technique confirms only by grepping its own delimiters out of the response body. The control puts the input in a single-quoted literal in a DELETE WHERE predicate, from which the vector cannot reach an unquoted, reflected projection; pgorm binds values and double-quotes identifiers, so no faithful control anywhere in this suite exposes one.",
          "evidence": {
            "kind": "no-boundary",
            "payload": "inline_query.xml",
            "where": [
              3
            ],
            "clause": [
              1,
              2,
              3,
              8
            ],
            "boundary": "boundaries.xml holds exactly one boundary whose <where> includes 3 (level 1, clause 0, ptype 1) and it carries an empty <prefix> and empty <suffix>; no where=3 boundary exists at any level that closes a ' or a \", so REPLACE can only land on an already-unquoted value.",
            "contexts": "4"
          }
        },
        "U": {
          "reason": "Every qualifying UNION test appends at where=1 and needs a SELECT set-operation slot to attach to. The control is an DELETE statement whose injection point is a single-quoted literal in a DELETE WHERE predicate; RETURNING cannot be unioned onto through the WHERE, so making this reachable would mean making the control a SELECT, which stops it being an DELETE control.",
          "evidence": {
            "kind": "no-attachable-position",
            "payload": "union_query.xml",
            "where": [
              1
            ],
            "clause": [
              1,
              2,
              3,
              4,
              5
            ],
            "boundary": "A level-3 ptype-2 `'` boundary closes the quote and the UNION payload supplies its own [GENERIC_SQL_COMMENT], but the enclosing INSERT/UPDATE/DELETE offers no set-operation position for the appended SELECT at any boundary level.",
            "contexts": "7a"
          }
        }
      }
    },
    {
      "id": "graph-root",
      "family": "Graph",
      "api": "Entity::graph/filter",
      "method": "GET",
      "route": "/case/protected/graph-root",
      "field": "input",
      "baseline": "alice",
      "alternate": "bob",
      "techniques": [
        "B",
        "E",
        "U",
        "S",
        "T",
        "Q"
      ],
      "controls": [
        "/case/control/graph-root"
      ],
      "fixed": {
        "table": "fixture.items",
        "tenant_guard": 1,
        "sentinel_id": 3
      },
      "inapplicable": {
        "Q": {
          "reason": "Inline-query tests are REPLACE-only (where=3), so the vector substitutes the whole parameter value with no prefix or suffix to escape quoting, and the technique confirms only by grepping its own delimiters out of the response body. The control puts the input in a single-quoted literal in a SELECT WHERE predicate, from which the vector cannot reach an unquoted, reflected projection; pgorm binds values and double-quotes identifiers, so no faithful control anywhere in this suite exposes one.",
          "evidence": {
            "kind": "no-boundary",
            "payload": "inline_query.xml",
            "where": [
              3
            ],
            "clause": [
              1,
              2,
              3,
              8
            ],
            "boundary": "boundaries.xml holds exactly one boundary whose <where> includes 3 (level 1, clause 0, ptype 1) and it carries an empty <prefix> and empty <suffix>; no where=3 boundary exists at any level that closes a ' or a \", so REPLACE can only land on an already-unquoted value.",
            "contexts": "4"
          }
        }
      }
    },
    {
      "id": "graph-joined",
      "family": "Graph",
      "api": "SelectGraph::join_one_as/filter",
      "method": "GET",
      "route": "/case/protected/graph-joined",
      "field": "input",
      "baseline": "alice",
      "alternate": "bob",
      "techniques": [
        "B",
        "E",
        "U",
        "S",
        "T",
        "Q"
      ],
      "controls": [
        "/case/control/graph-joined"
      ],
      "fixed": {
        "table": "fixture.items",
        "tenant_guard": 1,
        "sentinel_id": 3
      },
      "inapplicable": {
        "Q": {
          "reason": "Inline-query tests are REPLACE-only (where=3), so the vector substitutes the whole parameter value with no prefix or suffix to escape quoting, and the technique confirms only by grepping its own delimiters out of the response body. The control puts the input in a single-quoted literal in a SELECT WHERE predicate, from which the vector cannot reach an unquoted, reflected projection; pgorm binds values and double-quotes identifiers, so no faithful control anywhere in this suite exposes one.",
          "evidence": {
            "kind": "no-boundary",
            "payload": "inline_query.xml",
            "where": [
              3
            ],
            "clause": [
              1,
              2,
              3,
              8
            ],
            "boundary": "boundaries.xml holds exactly one boundary whose <where> includes 3 (level 1, clause 0, ptype 1) and it carries an empty <prefix> and empty <suffix>; no where=3 boundary exists at any level that closes a ' or a \", so REPLACE can only land on an already-unquoted value.",
            "contexts": "4"
          }
        }
      }
    },
    {
      "id": "graph-alias",
      "family": "Graph",
      "api": "SelectGraph::join_one_as alias",
      "method": "GET",
      "route": "/case/protected/graph-alias",
      "field": "input",
      "baseline": "peer",
      "alternate": "other_peer",
      "techniques": [
        "B",
        "E",
        "U",
        "S",
        "T",
        "Q"
      ],
      "controls": [
        "/case/control/graph-alias"
      ],
      "fixed": {
        "table": "fixture.items",
        "tenant_guard": 1,
        "sentinel_id": 3
      },
      "inapplicable": {
        "B": {
          "reason": "The control puts the input in a double-quoted table alias after AS. Once a boundary closes the app's double quote the injection point is a FROM/alias/ORDER BY position, which has no truth slot for an `AND <inference>` predicate \u2014 the shape every qualifying where=1 test needs. Only a set operation or a statement terminator can attach there.",
          "evidence": {
            "kind": "no-attachable-position",
            "payload": "boolean_blind.xml",
            "where": [
              1,
              3
            ],
            "clause": [
              1,
              2,
              3,
              4,
              5,
              6,
              7,
              8,
              9
            ],
            "boundary": "The ptype-4 `\"` boundaries reachable at level 3 close the identifier but leave the injection point in a FROM/alias/ORDER BY position, and no boundary at any level relocates that position into a truth context for a where=1 append.",
            "contexts": "7a"
          }
        },
        "E": {
          "reason": "The control puts the input in a double-quoted table alias after AS. Once a boundary closes the app's double quote the injection point is a FROM/alias/ORDER BY position, which has no truth slot for an error-forcing subquery \u2014 the shape every qualifying where=1 test needs. Only a set operation or a statement terminator can attach there.",
          "evidence": {
            "kind": "no-attachable-position",
            "payload": "error_based.xml",
            "where": [
              1,
              3
            ],
            "clause": [
              1,
              2,
              3,
              8,
              9
            ],
            "boundary": "The ptype-4 `\"` boundaries reachable at level 3 close the identifier but leave the injection point in a FROM/alias/ORDER BY position, and no boundary at any level relocates that position into a truth context for a where=1 append.",
            "contexts": "7a"
          }
        },
        "Q": {
          "reason": "Inline-query tests are REPLACE-only (where=3), so the vector substitutes the whole parameter value with no prefix or suffix to escape quoting, and the technique confirms only by grepping its own delimiters out of the response body. The control puts the input in a double-quoted table alias after AS, from which the vector cannot reach an unquoted, reflected projection; pgorm binds values and double-quotes identifiers, so no faithful control anywhere in this suite exposes one.",
          "evidence": {
            "kind": "no-boundary",
            "payload": "inline_query.xml",
            "where": [
              3
            ],
            "clause": [
              1,
              2,
              3,
              8
            ],
            "boundary": "boundaries.xml holds exactly one boundary whose <where> includes 3 (level 1, clause 0, ptype 1) and it carries an empty <prefix> and empty <suffix>; no where=3 boundary exists at any level that closes a ' or a \", so REPLACE can only land on an already-unquoted value.",
            "contexts": "4"
          }
        },
        "T": {
          "reason": "The control puts the input in a double-quoted table alias after AS. Once a boundary closes the app's double quote the injection point is a FROM/alias/ORDER BY position, which has no truth slot for a timing subquery \u2014 the shape every qualifying where=1 test needs. Only a set operation or a statement terminator can attach there.",
          "evidence": {
            "kind": "no-attachable-position",
            "payload": "time_blind.xml",
            "where": [
              1,
              3
            ],
            "clause": [
              1,
              2,
              3,
              8,
              9
            ],
            "boundary": "The ptype-4 `\"` boundaries reachable at level 3 close the identifier but leave the injection point in a FROM/alias/ORDER BY position, and no boundary at any level relocates that position into a truth context for a where=1 append.",
            "contexts": "7a"
          }
        }
      }
    },
    {
      "id": "pipeline-literal",
      "family": "Pipeline",
      "api": "Pipeline::filter inline",
      "method": "GET",
      "route": "/case/protected/pipeline-literal",
      "field": "input",
      "baseline": "alice",
      "alternate": "bob",
      "techniques": [
        "B",
        "E",
        "U",
        "S",
        "T",
        "Q"
      ],
      "controls": [
        "/case/control/pipeline-literal"
      ],
      "fixed": {
        "table": "fixture.items",
        "tenant_guard": 1,
        "sentinel_id": 3
      },
      "inapplicable": {
        "Q": {
          "reason": "Inline-query tests are REPLACE-only (where=3), so the vector substitutes the whole parameter value with no prefix or suffix to escape quoting, and the technique confirms only by grepping its own delimiters out of the response body. The control puts the input in a single-quoted literal in a SELECT WHERE predicate, from which the vector cannot reach an unquoted, reflected projection; pgorm binds values and double-quotes identifiers, so no faithful control anywhere in this suite exposes one.",
          "evidence": {
            "kind": "no-boundary",
            "payload": "inline_query.xml",
            "where": [
              3
            ],
            "clause": [
              1,
              2,
              3,
              8
            ],
            "boundary": "boundaries.xml holds exactly one boundary whose <where> includes 3 (level 1, clause 0, ptype 1) and it carries an empty <prefix> and empty <suffix>; no where=3 boundary exists at any level that closes a ' or a \", so REPLACE can only land on an already-unquoted value.",
            "contexts": "4"
          }
        }
      }
    },
    {
      "id": "pipeline-bound",
      "family": "Pipeline",
      "api": "Pipeline::filter_with binder",
      "method": "GET",
      "route": "/case/protected/pipeline-bound",
      "field": "input",
      "baseline": "alice",
      "alternate": "bob",
      "techniques": [
        "B",
        "E",
        "U",
        "S",
        "T",
        "Q"
      ],
      "controls": [
        "/case/control/pipeline-bound"
      ],
      "fixed": {
        "table": "fixture.items",
        "tenant_guard": 1,
        "sentinel_id": 3
      },
      "inapplicable": {
        "Q": {
          "reason": "Inline-query tests are REPLACE-only (where=3), so the vector substitutes the whole parameter value with no prefix or suffix to escape quoting, and the technique confirms only by grepping its own delimiters out of the response body. The control puts the input in a single-quoted literal in a SELECT WHERE predicate, from which the vector cannot reach an unquoted, reflected projection; pgorm binds values and double-quotes identifiers, so no faithful control anywhere in this suite exposes one.",
          "evidence": {
            "kind": "no-boundary",
            "payload": "inline_query.xml",
            "where": [
              3
            ],
            "clause": [
              1,
              2,
              3,
              8
            ],
            "boundary": "boundaries.xml holds exactly one boundary whose <where> includes 3 (level 1, clause 0, ptype 1) and it carries an empty <prefix> and empty <suffix>; no where=3 boundary exists at any level that closes a ' or a \", so REPLACE can only land on an already-unquoted value.",
            "contexts": "4"
          }
        }
      }
    },
    {
      "id": "pipeline-projection",
      "family": "Pipeline",
      "api": "Pipeline::select identifier",
      "method": "GET",
      "route": "/case/protected/pipeline-projection",
      "field": "input",
      "baseline": "name",
      "alternate": "note",
      "techniques": [
        "B",
        "E",
        "U",
        "S",
        "T",
        "Q"
      ],
      "controls": [
        "/case/control/pipeline-projection"
      ],
      "fixed": {
        "table": "fixture.items",
        "tenant_guard": 1,
        "sentinel_id": 3
      },
      "inapplicable": {
        "E": {
          "reason": "The control puts the input in a double-quoted leading projected identifier, a projected column that depends on the trailing `FROM items`. Attaching an error-forcing subquery fails because appending an error subquery makes `SELECT (text AND bool)` a type error. The only faithful repair moves the identifier to a trailing slot, which changes what the case tests.",
          "evidence": {
            "kind": "no-attachable-position",
            "payload": "error_based.xml",
            "where": [
              1,
              3
            ],
            "clause": [
              1,
              2,
              3,
              8,
              9
            ],
            "boundary": "Every level-3 boundary either comments forward, deleting the `FROM items` the projected name resolves against, or appends a predicate, making `SELECT (text AND bool)` a type error; the clause-8 identifier boundaries are level 4-5 and rescue no technique but B.",
            "contexts": "7a"
          }
        },
        "Q": {
          "reason": "Inline-query tests are REPLACE-only (where=3), so the vector substitutes the whole parameter value with no prefix or suffix to escape quoting, and the technique confirms only by grepping its own delimiters out of the response body. The control puts the input in a double-quoted leading projected identifier, from which the vector cannot reach an unquoted, reflected projection; pgorm binds values and double-quotes identifiers, so no faithful control anywhere in this suite exposes one.",
          "evidence": {
            "kind": "no-boundary",
            "payload": "inline_query.xml",
            "where": [
              3
            ],
            "clause": [
              1,
              2,
              3,
              8
            ],
            "boundary": "boundaries.xml holds exactly one boundary whose <where> includes 3 (level 1, clause 0, ptype 1) and it carries an empty <prefix> and empty <suffix>; no where=3 boundary exists at any level that closes a ' or a \", so REPLACE can only land on an already-unquoted value.",
            "contexts": "4"
          }
        },
        "S": {
          "reason": "The control puts the input in a double-quoted leading projected identifier, a projected column that depends on the trailing `FROM items`. Attaching a `;<statement>` batch continuation fails because the first statement of the batch loses its `FROM items` to the payload's own comment, so the batch errors before the appended statement runs. The only faithful repair moves the identifier to a trailing slot, which changes what the case tests.",
          "evidence": {
            "kind": "no-attachable-position",
            "payload": "stacked_queries.xml",
            "where": [
              1
            ],
            "clause": [
              1,
              2,
              3,
              4,
              5,
              6,
              7,
              8
            ],
            "boundary": "Every level-3 boundary either comments forward, deleting the `FROM items` the projected name resolves against, or appends a predicate, making `SELECT (text AND bool)` a type error; the clause-8 identifier boundaries are level 4-5 and rescue no technique but B.",
            "contexts": "7a"
          }
        },
        "T": {
          "reason": "The control puts the input in a double-quoted leading projected identifier, a projected column that depends on the trailing `FROM items`. Attaching a timing subquery fails because appending a timing subquery makes `SELECT (text AND bool)` a type error. The only faithful repair moves the identifier to a trailing slot, which changes what the case tests.",
          "evidence": {
            "kind": "no-attachable-position",
            "payload": "time_blind.xml",
            "where": [
              1,
              3
            ],
            "clause": [
              1,
              2,
              3,
              8,
              9
            ],
            "boundary": "Every level-3 boundary either comments forward, deleting the `FROM items` the projected name resolves against, or appends a predicate, making `SELECT (text AND bool)` a type error; the clause-8 identifier boundaries are level 4-5 and rescue no technique but B.",
            "contexts": "7a"
          }
        },
        "U": {
          "reason": "The control puts the input in a double-quoted leading projected identifier, a projected column that depends on the trailing `FROM items`. Attaching a `UNION SELECT` set operation fails because commenting forward deletes the `FROM items` the UNION's left statement needs. The only faithful repair moves the identifier to a trailing slot, which changes what the case tests.",
          "evidence": {
            "kind": "no-attachable-position",
            "payload": "union_query.xml",
            "where": [
              1
            ],
            "clause": [
              1,
              2,
              3,
              4,
              5
            ],
            "boundary": "Every level-3 boundary either comments forward, deleting the `FROM items` the projected name resolves against, or appends a predicate, making `SELECT (text AND bool)` a type error; the clause-8 identifier boundaries are level 4-5 and rescue no technique but B.",
            "contexts": "7a"
          }
        }
      }
    },
    {
      "id": "pipeline-sources",
      "family": "Pipeline",
      "api": "Pipeline::join/select_sources",
      "method": "GET",
      "route": "/case/protected/pipeline-sources",
      "field": "input",
      "baseline": "alice",
      "alternate": "bob",
      "techniques": [
        "B",
        "E",
        "U",
        "S",
        "T",
        "Q"
      ],
      "controls": [
        "/case/control/pipeline-sources"
      ],
      "fixed": {
        "table": "fixture.items",
        "tenant_guard": 1,
        "sentinel_id": 3
      },
      "inapplicable": {
        "Q": {
          "reason": "Inline-query tests are REPLACE-only (where=3), so the vector substitutes the whole parameter value with no prefix or suffix to escape quoting, and the technique confirms only by grepping its own delimiters out of the response body. The control puts the input in a single-quoted literal in a SELECT WHERE predicate, from which the vector cannot reach an unquoted, reflected projection; pgorm binds values and double-quotes identifiers, so no faithful control anywhere in this suite exposes one.",
          "evidence": {
            "kind": "no-boundary",
            "payload": "inline_query.xml",
            "where": [
              3
            ],
            "clause": [
              1,
              2,
              3,
              8
            ],
            "boundary": "boundaries.xml holds exactly one boundary whose <where> includes 3 (level 1, clause 0, ptype 1) and it carries an empty <prefix> and empty <suffix>; no where=3 boundary exists at any level that closes a ' or a \", so REPLACE can only land on an already-unquoted value.",
            "contexts": "4"
          }
        }
      }
    },
    {
      "id": "schema",
      "family": "Object names",
      "api": "SelectStatement::from schema",
      "method": "GET",
      "route": "/case/protected/schema",
      "field": "input",
      "baseline": "fixture",
      "alternate": "other",
      "techniques": [
        "B",
        "E",
        "U",
        "S",
        "T",
        "Q"
      ],
      "controls": [
        "/case/control/schema"
      ],
      "fixed": {
        "table": "fixture.items",
        "tenant_guard": 1,
        "sentinel_id": 3
      },
      "inapplicable": {
        "B": {
          "reason": "The control puts the input in a double-quoted schema name whose trailing `.items` is bound to it. Attaching an `AND <inference>` predicate needs the boundary to either comment `.items` away, leaving a schema where a relation must be, or to inject between the identifier and `.items`, which is a syntax error.",
          "evidence": {
            "kind": "no-attachable-position",
            "payload": "boolean_blind.xml",
            "where": [
              1,
              3
            ],
            "clause": [
              1,
              2,
              3,
              4,
              5,
              6,
              7,
              8,
              9
            ],
            "boundary": "Every boundary either comments `.items` away, leaving `FROM \"schema\"`, which is not a relation, or appends between the identifier and `.items`, which is a syntax error; no boundary at any level reconstructs the qualified name.",
            "contexts": "7a"
          }
        },
        "E": {
          "reason": "The control puts the input in a double-quoted schema name whose trailing `.items` is bound to it. Attaching an error-forcing subquery needs the boundary to either comment `.items` away, leaving a schema where a relation must be, or to inject between the identifier and `.items`, which is a syntax error.",
          "evidence": {
            "kind": "no-attachable-position",
            "payload": "error_based.xml",
            "where": [
              1,
              3
            ],
            "clause": [
              1,
              2,
              3,
              8,
              9
            ],
            "boundary": "Every boundary either comments `.items` away, leaving `FROM \"schema\"`, which is not a relation, or appends between the identifier and `.items`, which is a syntax error; no boundary at any level reconstructs the qualified name.",
            "contexts": "7a"
          }
        },
        "Q": {
          "reason": "Inline-query tests are REPLACE-only (where=3), so the vector substitutes the whole parameter value with no prefix or suffix to escape quoting, and the technique confirms only by grepping its own delimiters out of the response body. The control puts the input in a double-quoted schema name bound to a trailing `.items`, from which the vector cannot reach an unquoted, reflected projection; pgorm binds values and double-quotes identifiers, so no faithful control anywhere in this suite exposes one.",
          "evidence": {
            "kind": "no-boundary",
            "payload": "inline_query.xml",
            "where": [
              3
            ],
            "clause": [
              1,
              2,
              3,
              8
            ],
            "boundary": "boundaries.xml holds exactly one boundary whose <where> includes 3 (level 1, clause 0, ptype 1) and it carries an empty <prefix> and empty <suffix>; no where=3 boundary exists at any level that closes a ' or a \", so REPLACE can only land on an already-unquoted value.",
            "contexts": "4"
          }
        },
        "S": {
          "reason": "The control puts the input in a double-quoted schema name whose trailing `.items` is bound to it. Attaching a `;<statement>` batch continuation needs the boundary to either comment `.items` away, leaving a schema where a relation must be, or to inject between the identifier and `.items`, which is a syntax error.",
          "evidence": {
            "kind": "no-attachable-position",
            "payload": "stacked_queries.xml",
            "where": [
              1
            ],
            "clause": [
              1,
              2,
              3,
              4,
              5,
              6,
              7,
              8
            ],
            "boundary": "Every boundary either comments `.items` away, leaving `FROM \"schema\"`, which is not a relation, or appends between the identifier and `.items`, which is a syntax error; no boundary at any level reconstructs the qualified name.",
            "contexts": "7a"
          }
        },
        "T": {
          "reason": "The control puts the input in a double-quoted schema name whose trailing `.items` is bound to it. Attaching a timing subquery needs the boundary to either comment `.items` away, leaving a schema where a relation must be, or to inject between the identifier and `.items`, which is a syntax error.",
          "evidence": {
            "kind": "no-attachable-position",
            "payload": "time_blind.xml",
            "where": [
              1,
              3
            ],
            "clause": [
              1,
              2,
              3,
              8,
              9
            ],
            "boundary": "Every boundary either comments `.items` away, leaving `FROM \"schema\"`, which is not a relation, or appends between the identifier and `.items`, which is a syntax error; no boundary at any level reconstructs the qualified name.",
            "contexts": "7a"
          }
        },
        "U": {
          "reason": "The control puts the input in a double-quoted schema name whose trailing `.items` is bound to it. Attaching a `UNION SELECT` set operation needs the boundary to either comment `.items` away, leaving a schema where a relation must be, or to inject between the identifier and `.items`, which is a syntax error.",
          "evidence": {
            "kind": "no-attachable-position",
            "payload": "union_query.xml",
            "where": [
              1
            ],
            "clause": [
              1,
              2,
              3,
              4,
              5
            ],
            "boundary": "Every boundary either comments `.items` away, leaving `FROM \"schema\"`, which is not a relation, or appends between the identifier and `.items`, which is a syntax error; no boundary at any level reconstructs the qualified name.",
            "contexts": "7a"
          }
        }
      }
    },
    {
      "id": "table",
      "family": "Object names",
      "api": "SelectStatement::from table",
      "method": "GET",
      "route": "/case/protected/table",
      "field": "input",
      "baseline": "items",
      "alternate": "other.items",
      "techniques": [
        "B",
        "E",
        "U",
        "S",
        "T",
        "Q"
      ],
      "controls": [
        "/case/control/table"
      ],
      "fixed": {
        "table": "fixture.items",
        "tenant_guard": 1,
        "sentinel_id": 3
      },
      "inapplicable": {
        "B": {
          "reason": "The control puts the input in a double-quoted relation name closing the FROM clause. Once a boundary closes the app's double quote the injection point is a FROM/alias/ORDER BY position, which has no truth slot for an `AND <inference>` predicate \u2014 the shape every qualifying where=1 test needs. Only a set operation or a statement terminator can attach there.",
          "evidence": {
            "kind": "no-attachable-position",
            "payload": "boolean_blind.xml",
            "where": [
              1,
              3
            ],
            "clause": [
              1,
              2,
              3,
              4,
              5,
              6,
              7,
              8,
              9
            ],
            "boundary": "The ptype-4 `\"` boundaries reachable at level 3 close the identifier but leave the injection point in a FROM/alias/ORDER BY position, and no boundary at any level relocates that position into a truth context for a where=1 append.",
            "contexts": "7a"
          }
        },
        "E": {
          "reason": "The control puts the input in a double-quoted relation name closing the FROM clause. Once a boundary closes the app's double quote the injection point is a FROM/alias/ORDER BY position, which has no truth slot for an error-forcing subquery \u2014 the shape every qualifying where=1 test needs. Only a set operation or a statement terminator can attach there.",
          "evidence": {
            "kind": "no-attachable-position",
            "payload": "error_based.xml",
            "where": [
              1,
              3
            ],
            "clause": [
              1,
              2,
              3,
              8,
              9
            ],
            "boundary": "The ptype-4 `\"` boundaries reachable at level 3 close the identifier but leave the injection point in a FROM/alias/ORDER BY position, and no boundary at any level relocates that position into a truth context for a where=1 append.",
            "contexts": "7a"
          }
        },
        "Q": {
          "reason": "Inline-query tests are REPLACE-only (where=3), so the vector substitutes the whole parameter value with no prefix or suffix to escape quoting, and the technique confirms only by grepping its own delimiters out of the response body. The control puts the input in a double-quoted relation name closing the FROM clause, from which the vector cannot reach an unquoted, reflected projection; pgorm binds values and double-quotes identifiers, so no faithful control anywhere in this suite exposes one.",
          "evidence": {
            "kind": "no-boundary",
            "payload": "inline_query.xml",
            "where": [
              3
            ],
            "clause": [
              1,
              2,
              3,
              8
            ],
            "boundary": "boundaries.xml holds exactly one boundary whose <where> includes 3 (level 1, clause 0, ptype 1) and it carries an empty <prefix> and empty <suffix>; no where=3 boundary exists at any level that closes a ' or a \", so REPLACE can only land on an already-unquoted value.",
            "contexts": "4"
          }
        },
        "T": {
          "reason": "The control puts the input in a double-quoted relation name closing the FROM clause. Once a boundary closes the app's double quote the injection point is a FROM/alias/ORDER BY position, which has no truth slot for a timing subquery \u2014 the shape every qualifying where=1 test needs. Only a set operation or a statement terminator can attach there.",
          "evidence": {
            "kind": "no-attachable-position",
            "payload": "time_blind.xml",
            "where": [
              1,
              3
            ],
            "clause": [
              1,
              2,
              3,
              8,
              9
            ],
            "boundary": "The ptype-4 `\"` boundaries reachable at level 3 close the identifier but leave the injection point in a FROM/alias/ORDER BY position, and no boundary at any level relocates that position into a truth context for a where=1 append.",
            "contexts": "7a"
          }
        }
      }
    },
    {
      "id": "column",
      "family": "Object names",
      "api": "SelectStatement::column",
      "method": "GET",
      "route": "/case/protected/column",
      "field": "input",
      "baseline": "name",
      "alternate": "note",
      "techniques": [
        "B",
        "E",
        "U",
        "S",
        "T",
        "Q"
      ],
      "controls": [
        "/case/control/column"
      ],
      "fixed": {
        "table": "fixture.items",
        "tenant_guard": 1,
        "sentinel_id": 3
      },
      "inapplicable": {
        "E": {
          "reason": "The control puts the input in a double-quoted leading projected identifier, a projected column that depends on the trailing `FROM items`. Attaching an error-forcing subquery fails because appending an error subquery makes `SELECT (text AND bool)` a type error. The only faithful repair moves the identifier to a trailing slot, which changes what the case tests.",
          "evidence": {
            "kind": "no-attachable-position",
            "payload": "error_based.xml",
            "where": [
              1,
              3
            ],
            "clause": [
              1,
              2,
              3,
              8,
              9
            ],
            "boundary": "Every level-3 boundary either comments forward, deleting the `FROM items` the projected name resolves against, or appends a predicate, making `SELECT (text AND bool)` a type error; the clause-8 identifier boundaries are level 4-5 and rescue no technique but B.",
            "contexts": "7a"
          }
        },
        "Q": {
          "reason": "Inline-query tests are REPLACE-only (where=3), so the vector substitutes the whole parameter value with no prefix or suffix to escape quoting, and the technique confirms only by grepping its own delimiters out of the response body. The control puts the input in a double-quoted leading projected identifier, from which the vector cannot reach an unquoted, reflected projection; pgorm binds values and double-quotes identifiers, so no faithful control anywhere in this suite exposes one.",
          "evidence": {
            "kind": "no-boundary",
            "payload": "inline_query.xml",
            "where": [
              3
            ],
            "clause": [
              1,
              2,
              3,
              8
            ],
            "boundary": "boundaries.xml holds exactly one boundary whose <where> includes 3 (level 1, clause 0, ptype 1) and it carries an empty <prefix> and empty <suffix>; no where=3 boundary exists at any level that closes a ' or a \", so REPLACE can only land on an already-unquoted value.",
            "contexts": "4"
          }
        },
        "S": {
          "reason": "The control puts the input in a double-quoted leading projected identifier, a projected column that depends on the trailing `FROM items`. Attaching a `;<statement>` batch continuation fails because the first statement of the batch loses its `FROM items` to the payload's own comment, so the batch errors before the appended statement runs. The only faithful repair moves the identifier to a trailing slot, which changes what the case tests.",
          "evidence": {
            "kind": "no-attachable-position",
            "payload": "stacked_queries.xml",
            "where": [
              1
            ],
            "clause": [
              1,
              2,
              3,
              4,
              5,
              6,
              7,
              8
            ],
            "boundary": "Every level-3 boundary either comments forward, deleting the `FROM items` the projected name resolves against, or appends a predicate, making `SELECT (text AND bool)` a type error; the clause-8 identifier boundaries are level 4-5 and rescue no technique but B.",
            "contexts": "7a"
          }
        },
        "T": {
          "reason": "The control puts the input in a double-quoted leading projected identifier, a projected column that depends on the trailing `FROM items`. Attaching a timing subquery fails because appending a timing subquery makes `SELECT (text AND bool)` a type error. The only faithful repair moves the identifier to a trailing slot, which changes what the case tests.",
          "evidence": {
            "kind": "no-attachable-position",
            "payload": "time_blind.xml",
            "where": [
              1,
              3
            ],
            "clause": [
              1,
              2,
              3,
              8,
              9
            ],
            "boundary": "Every level-3 boundary either comments forward, deleting the `FROM items` the projected name resolves against, or appends a predicate, making `SELECT (text AND bool)` a type error; the clause-8 identifier boundaries are level 4-5 and rescue no technique but B.",
            "contexts": "7a"
          }
        },
        "U": {
          "reason": "The control puts the input in a double-quoted leading projected identifier, a projected column that depends on the trailing `FROM items`. Attaching a `UNION SELECT` set operation fails because commenting forward deletes the `FROM items` the UNION's left statement needs. The only faithful repair moves the identifier to a trailing slot, which changes what the case tests.",
          "evidence": {
            "kind": "no-attachable-position",
            "payload": "union_query.xml",
            "where": [
              1
            ],
            "clause": [
              1,
              2,
              3,
              4,
              5
            ],
            "boundary": "Every level-3 boundary either comments forward, deleting the `FROM items` the projected name resolves against, or appends a predicate, making `SELECT (text AND bool)` a type error; the clause-8 identifier boundaries are level 4-5 and rescue no technique but B.",
            "contexts": "7a"
          }
        }
      }
    },
    {
      "id": "alias",
      "family": "Object names",
      "api": "SelectStatement::expr_as",
      "method": "GET",
      "route": "/case/protected/alias",
      "field": "input",
      "baseline": "value",
      "alternate": "other_value",
      "techniques": [
        "B",
        "E",
        "U",
        "S",
        "T",
        "Q"
      ],
      "controls": [
        "/case/control/alias"
      ],
      "fixed": {
        "table": "fixture.items",
        "tenant_guard": 1,
        "sentinel_id": 3
      },
      "inapplicable": {
        "B": {
          "reason": "The control puts the input in a double-quoted table alias after AS. Once a boundary closes the app's double quote the injection point is a FROM/alias/ORDER BY position, which has no truth slot for an `AND <inference>` predicate \u2014 the shape every qualifying where=1 test needs. Only a set operation or a statement terminator can attach there.",
          "evidence": {
            "kind": "no-attachable-position",
            "payload": "boolean_blind.xml",
            "where": [
              1,
              3
            ],
            "clause": [
              1,
              2,
              3,
              4,
              5,
              6,
              7,
              8,
              9
            ],
            "boundary": "The ptype-4 `\"` boundaries reachable at level 3 close the identifier but leave the injection point in a FROM/alias/ORDER BY position, and no boundary at any level relocates that position into a truth context for a where=1 append.",
            "contexts": "7a"
          }
        },
        "E": {
          "reason": "The control puts the input in a double-quoted table alias after AS. Once a boundary closes the app's double quote the injection point is a FROM/alias/ORDER BY position, which has no truth slot for an error-forcing subquery \u2014 the shape every qualifying where=1 test needs. Only a set operation or a statement terminator can attach there.",
          "evidence": {
            "kind": "no-attachable-position",
            "payload": "error_based.xml",
            "where": [
              1,
              3
            ],
            "clause": [
              1,
              2,
              3,
              8,
              9
            ],
            "boundary": "The ptype-4 `\"` boundaries reachable at level 3 close the identifier but leave the injection point in a FROM/alias/ORDER BY position, and no boundary at any level relocates that position into a truth context for a where=1 append.",
            "contexts": "7a"
          }
        },
        "Q": {
          "reason": "Inline-query tests are REPLACE-only (where=3), so the vector substitutes the whole parameter value with no prefix or suffix to escape quoting, and the technique confirms only by grepping its own delimiters out of the response body. The control puts the input in a double-quoted table alias after AS, from which the vector cannot reach an unquoted, reflected projection; pgorm binds values and double-quotes identifiers, so no faithful control anywhere in this suite exposes one.",
          "evidence": {
            "kind": "no-boundary",
            "payload": "inline_query.xml",
            "where": [
              3
            ],
            "clause": [
              1,
              2,
              3,
              8
            ],
            "boundary": "boundaries.xml holds exactly one boundary whose <where> includes 3 (level 1, clause 0, ptype 1) and it carries an empty <prefix> and empty <suffix>; no where=3 boundary exists at any level that closes a ' or a \", so REPLACE can only land on an already-unquoted value.",
            "contexts": "4"
          }
        },
        "T": {
          "reason": "The control puts the input in a double-quoted table alias after AS. Once a boundary closes the app's double quote the injection point is a FROM/alias/ORDER BY position, which has no truth slot for a timing subquery \u2014 the shape every qualifying where=1 test needs. Only a set operation or a statement terminator can attach there.",
          "evidence": {
            "kind": "no-attachable-position",
            "payload": "time_blind.xml",
            "where": [
              1,
              3
            ],
            "clause": [
              1,
              2,
              3,
              8,
              9
            ],
            "boundary": "The ptype-4 `\"` boundaries reachable at level 3 close the identifier but leave the injection point in a FROM/alias/ORDER BY position, and no boundary at any level relocates that position into a truth context for a where=1 append.",
            "contexts": "7a"
          }
        }
      }
    },
    {
      "id": "order",
      "family": "Object names",
      "api": "SelectStatement::order_by",
      "method": "GET",
      "route": "/case/protected/order",
      "field": "input",
      "baseline": "name",
      "alternate": "note",
      "techniques": [
        "B",
        "E",
        "U",
        "S",
        "T",
        "Q"
      ],
      "controls": [
        "/case/control/order"
      ],
      "fixed": {
        "table": "fixture.items",
        "tenant_guard": 1,
        "sentinel_id": 3
      },
      "inapplicable": {
        "B": {
          "reason": "The control puts the input in a double-quoted identifier in the trailing ORDER BY. Once a boundary closes the app's double quote the injection point is a FROM/alias/ORDER BY position, which has no truth slot for an `AND <inference>` predicate \u2014 the shape every qualifying where=1 test needs. Only a set operation or a statement terminator can attach there.",
          "evidence": {
            "kind": "no-attachable-position",
            "payload": "boolean_blind.xml",
            "where": [
              1,
              3
            ],
            "clause": [
              1,
              2,
              3,
              4,
              5,
              6,
              7,
              8,
              9
            ],
            "boundary": "The ptype-4 `\"` boundaries reachable at level 3 close the identifier but leave the injection point in a FROM/alias/ORDER BY position, and no boundary at any level relocates that position into a truth context for a where=1 append.",
            "contexts": "7a"
          }
        },
        "E": {
          "reason": "The control puts the input in a double-quoted identifier in the trailing ORDER BY. Once a boundary closes the app's double quote the injection point is a FROM/alias/ORDER BY position, which has no truth slot for an error-forcing subquery \u2014 the shape every qualifying where=1 test needs. Only a set operation or a statement terminator can attach there.",
          "evidence": {
            "kind": "no-attachable-position",
            "payload": "error_based.xml",
            "where": [
              1,
              3
            ],
            "clause": [
              1,
              2,
              3,
              8,
              9
            ],
            "boundary": "The ptype-4 `\"` boundaries reachable at level 3 close the identifier but leave the injection point in a FROM/alias/ORDER BY position, and no boundary at any level relocates that position into a truth context for a where=1 append.",
            "contexts": "7a"
          }
        },
        "Q": {
          "reason": "Inline-query tests are REPLACE-only (where=3), so the vector substitutes the whole parameter value with no prefix or suffix to escape quoting, and the technique confirms only by grepping its own delimiters out of the response body. The control puts the input in a double-quoted identifier in the trailing ORDER BY, from which the vector cannot reach an unquoted, reflected projection; pgorm binds values and double-quotes identifiers, so no faithful control anywhere in this suite exposes one.",
          "evidence": {
            "kind": "no-boundary",
            "payload": "inline_query.xml",
            "where": [
              3
            ],
            "clause": [
              1,
              2,
              3,
              8
            ],
            "boundary": "boundaries.xml holds exactly one boundary whose <where> includes 3 (level 1, clause 0, ptype 1) and it carries an empty <prefix> and empty <suffix>; no where=3 boundary exists at any level that closes a ' or a \", so REPLACE can only land on an already-unquoted value.",
            "contexts": "4"
          }
        },
        "T": {
          "reason": "The control puts the input in a double-quoted identifier in the trailing ORDER BY. Once a boundary closes the app's double quote the injection point is a FROM/alias/ORDER BY position, which has no truth slot for a timing subquery \u2014 the shape every qualifying where=1 test needs. Only a set operation or a statement terminator can attach there.",
          "evidence": {
            "kind": "no-attachable-position",
            "payload": "time_blind.xml",
            "where": [
              1,
              3
            ],
            "clause": [
              1,
              2,
              3,
              8,
              9
            ],
            "boundary": "The ptype-4 `\"` boundaries reachable at level 3 close the identifier but leave the injection point in a FROM/alias/ORDER BY position, and no boundary at any level relocates that position into a truth context for a where=1 append.",
            "contexts": "7a"
          }
        },
        "U": {
          "reason": "UNION tests append at where=1 inside clause 1-5. The control puts the input in a double-quoted identifier in the trailing ORDER BY, and nothing may syntactically follow ORDER BY, LIMIT or OFFSET, so no UNION SELECT can attach while the injection point stays in that clause.",
          "evidence": {
            "kind": "no-attachable-position",
            "payload": "union_query.xml",
            "where": [
              1
            ],
            "clause": [
              1,
              2,
              3,
              4,
              5
            ],
            "boundary": "The ptype-1 and ptype-4 boundaries close the token, but a UNION SELECT appended after ORDER BY/LIMIT/OFFSET is a syntax error; no boundary relocates the injection point out of the terminal clause.",
            "contexts": "7a"
          }
        }
      }
    },
    {
      "id": "group",
      "family": "Object names",
      "api": "SelectStatement::group_by_col",
      "method": "GET",
      "route": "/case/protected/group",
      "field": "input",
      "baseline": "name",
      "alternate": "note",
      "techniques": [
        "B",
        "E",
        "U",
        "S",
        "T",
        "Q"
      ],
      "controls": [
        "/case/control/group"
      ],
      "fixed": {
        "table": "fixture.items",
        "tenant_guard": 1,
        "sentinel_id": 3
      },
      "inapplicable": {
        "E": {
          "reason": "The control puts the input in a double-quoted leading projected identifier, a projected column that depends on the trailing `FROM items`. Attaching an error-forcing subquery fails because appending an error subquery makes `SELECT (text AND bool)` a type error. The only faithful repair moves the identifier to a trailing slot, which changes what the case tests.",
          "evidence": {
            "kind": "no-attachable-position",
            "payload": "error_based.xml",
            "where": [
              1,
              3
            ],
            "clause": [
              1,
              2,
              3,
              8,
              9
            ],
            "boundary": "Every level-3 boundary either comments forward, deleting the `FROM items` the projected name resolves against, or appends a predicate, making `SELECT (text AND bool)` a type error; the clause-8 identifier boundaries are level 4-5 and rescue no technique but B.",
            "contexts": "7a"
          }
        },
        "Q": {
          "reason": "Inline-query tests are REPLACE-only (where=3), so the vector substitutes the whole parameter value with no prefix or suffix to escape quoting, and the technique confirms only by grepping its own delimiters out of the response body. The control puts the input in a double-quoted leading projected identifier, from which the vector cannot reach an unquoted, reflected projection; pgorm binds values and double-quotes identifiers, so no faithful control anywhere in this suite exposes one.",
          "evidence": {
            "kind": "no-boundary",
            "payload": "inline_query.xml",
            "where": [
              3
            ],
            "clause": [
              1,
              2,
              3,
              8
            ],
            "boundary": "boundaries.xml holds exactly one boundary whose <where> includes 3 (level 1, clause 0, ptype 1) and it carries an empty <prefix> and empty <suffix>; no where=3 boundary exists at any level that closes a ' or a \", so REPLACE can only land on an already-unquoted value.",
            "contexts": "4"
          }
        },
        "S": {
          "reason": "The control puts the input in a double-quoted leading projected identifier, a projected column that depends on the trailing `FROM items`. Attaching a `;<statement>` batch continuation fails because the first statement of the batch loses its `FROM items` to the payload's own comment, so the batch errors before the appended statement runs. The only faithful repair moves the identifier to a trailing slot, which changes what the case tests.",
          "evidence": {
            "kind": "no-attachable-position",
            "payload": "stacked_queries.xml",
            "where": [
              1
            ],
            "clause": [
              1,
              2,
              3,
              4,
              5,
              6,
              7,
              8
            ],
            "boundary": "Every level-3 boundary either comments forward, deleting the `FROM items` the projected name resolves against, or appends a predicate, making `SELECT (text AND bool)` a type error; the clause-8 identifier boundaries are level 4-5 and rescue no technique but B.",
            "contexts": "7a"
          }
        },
        "T": {
          "reason": "The control puts the input in a double-quoted leading projected identifier, a projected column that depends on the trailing `FROM items`. Attaching a timing subquery fails because appending a timing subquery makes `SELECT (text AND bool)` a type error. The only faithful repair moves the identifier to a trailing slot, which changes what the case tests.",
          "evidence": {
            "kind": "no-attachable-position",
            "payload": "time_blind.xml",
            "where": [
              1,
              3
            ],
            "clause": [
              1,
              2,
              3,
              8,
              9
            ],
            "boundary": "Every level-3 boundary either comments forward, deleting the `FROM items` the projected name resolves against, or appends a predicate, making `SELECT (text AND bool)` a type error; the clause-8 identifier boundaries are level 4-5 and rescue no technique but B.",
            "contexts": "7a"
          }
        },
        "U": {
          "reason": "The control puts the input in a double-quoted leading projected identifier, a projected column that depends on the trailing `FROM items`. Attaching a `UNION SELECT` set operation fails because commenting forward deletes the `FROM items` the UNION's left statement needs. The only faithful repair moves the identifier to a trailing slot, which changes what the case tests.",
          "evidence": {
            "kind": "no-attachable-position",
            "payload": "union_query.xml",
            "where": [
              1
            ],
            "clause": [
              1,
              2,
              3,
              4,
              5
            ],
            "boundary": "Every level-3 boundary either comments forward, deleting the `FROM items` the projected name resolves against, or appends a predicate, making `SELECT (text AND bool)` a type error; the clause-8 identifier boundaries are level 4-5 and rescue no technique but B.",
            "contexts": "7a"
          }
        }
      }
    },
    {
      "id": "function",
      "family": "Type/function names",
      "api": "Func::cust",
      "method": "GET",
      "route": "/case/protected/function",
      "field": "input",
      "baseline": "upper",
      "alternate": "lower",
      "techniques": [
        "B",
        "E",
        "U",
        "S",
        "T",
        "Q"
      ],
      "controls": [
        "/case/control/function"
      ],
      "fixed": {
        "table": "fixture.items",
        "tenant_guard": 1,
        "sentinel_id": 3
      },
      "inapplicable": {
        "B": {
          "reason": "The control puts the input in an unquoted callable name immediately followed by `('Alice')`. Attaching an `AND <inference>` predicate leaves an expression in the callable position, and PostgreSQL rejects calling an expression result as a function before any payload is evaluated.",
          "evidence": {
            "kind": "no-attachable-position",
            "payload": "boolean_blind.xml",
            "where": [
              1,
              3
            ],
            "clause": [
              1,
              2,
              3,
              4,
              5,
              6,
              7,
              8,
              9
            ],
            "boundary": "No boundary in the catalogue closes out of a function-name slot: appending or replacing yields `<expr>('Alice')`, calling an expression result as a function, which PostgreSQL rejects before any payload can be evaluated.",
            "contexts": "7a"
          }
        },
        "E": {
          "reason": "The control puts the input in an unquoted callable name immediately followed by `('Alice')`. Attaching an error-forcing subquery leaves an expression in the callable position, and PostgreSQL rejects calling an expression result as a function before any payload is evaluated.",
          "evidence": {
            "kind": "no-attachable-position",
            "payload": "error_based.xml",
            "where": [
              1,
              3
            ],
            "clause": [
              1,
              2,
              3,
              8,
              9
            ],
            "boundary": "No boundary in the catalogue closes out of a function-name slot: appending or replacing yields `<expr>('Alice')`, calling an expression result as a function, which PostgreSQL rejects before any payload can be evaluated.",
            "contexts": "7a"
          }
        },
        "Q": {
          "reason": "Inline-query tests are REPLACE-only (where=3), so the vector substitutes the whole parameter value with no prefix or suffix to escape quoting, and the technique confirms only by grepping its own delimiters out of the response body. The control puts the input in an unquoted callable name immediately followed by `(`, from which the vector cannot reach an unquoted, reflected projection; pgorm binds values and double-quotes identifiers, so no faithful control anywhere in this suite exposes one.",
          "evidence": {
            "kind": "no-boundary",
            "payload": "inline_query.xml",
            "where": [
              3
            ],
            "clause": [
              1,
              2,
              3,
              8
            ],
            "boundary": "boundaries.xml holds exactly one boundary whose <where> includes 3 (level 1, clause 0, ptype 1) and it carries an empty <prefix> and empty <suffix>; no where=3 boundary exists at any level that closes a ' or a \", so REPLACE can only land on an already-unquoted value.",
            "contexts": "4"
          }
        },
        "S": {
          "reason": "The control puts the input in an unquoted callable name immediately followed by `('Alice')`. Attaching a `;<statement>` batch continuation leaves an expression in the callable position, and PostgreSQL rejects calling an expression result as a function before any payload is evaluated.",
          "evidence": {
            "kind": "no-attachable-position",
            "payload": "stacked_queries.xml",
            "where": [
              1
            ],
            "clause": [
              1,
              2,
              3,
              4,
              5,
              6,
              7,
              8
            ],
            "boundary": "No boundary in the catalogue closes out of a function-name slot: appending or replacing yields `<expr>('Alice')`, calling an expression result as a function, which PostgreSQL rejects before any payload can be evaluated.",
            "contexts": "7a"
          }
        },
        "T": {
          "reason": "The control puts the input in an unquoted callable name immediately followed by `('Alice')`. Attaching a timing subquery leaves an expression in the callable position, and PostgreSQL rejects calling an expression result as a function before any payload is evaluated.",
          "evidence": {
            "kind": "no-attachable-position",
            "payload": "time_blind.xml",
            "where": [
              1,
              3
            ],
            "clause": [
              1,
              2,
              3,
              8,
              9
            ],
            "boundary": "No boundary in the catalogue closes out of a function-name slot: appending or replacing yields `<expr>('Alice')`, calling an expression result as a function, which PostgreSQL rejects before any payload can be evaluated.",
            "contexts": "7a"
          }
        },
        "U": {
          "reason": "The control puts the input in an unquoted callable name immediately followed by `('Alice')`. Attaching a `UNION SELECT` set operation leaves an expression in the callable position, and PostgreSQL rejects calling an expression result as a function before any payload is evaluated.",
          "evidence": {
            "kind": "no-attachable-position",
            "payload": "union_query.xml",
            "where": [
              1
            ],
            "clause": [
              1,
              2,
              3,
              4,
              5
            ],
            "boundary": "No boundary in the catalogue closes out of a function-name slot: appending or replacing yields `<expr>('Alice')`, calling an expression result as a function, which PostgreSQL rejects before any payload can be evaluated.",
            "contexts": "7a"
          }
        }
      }
    },
    {
      "id": "cast",
      "family": "Type/function names",
      "api": "Expr::cast_as",
      "method": "GET",
      "route": "/case/protected/cast",
      "field": "input",
      "baseline": "text",
      "alternate": "QuotedType",
      "techniques": [
        "B",
        "E",
        "U",
        "S",
        "T",
        "Q"
      ],
      "controls": [
        "/case/control/cast"
      ],
      "fixed": {
        "table": "fixture.items",
        "tenant_guard": 1,
        "sentinel_id": 3
      },
      "inapplicable": {
        "Q": {
          "reason": "Inline-query tests are REPLACE-only (where=3), so the vector substitutes the whole parameter value with no prefix or suffix to escape quoting, and the technique confirms only by grepping its own delimiters out of the response body. The control puts the input in an unquoted type name inside CAST(... AS ...), from which the vector cannot reach an unquoted, reflected projection; pgorm binds values and double-quotes identifiers, so no faithful control anywhere in this suite exposes one.",
          "evidence": {
            "kind": "no-boundary",
            "payload": "inline_query.xml",
            "where": [
              3
            ],
            "clause": [
              1,
              2,
              3,
              8
            ],
            "boundary": "boundaries.xml holds exactly one boundary whose <where> includes 3 (level 1, clause 0, ptype 1) and it carries an empty <prefix> and empty <suffix>; no where=3 boundary exists at any level that closes a ' or a \", so REPLACE can only land on an already-unquoted value.",
            "contexts": "4"
          }
        }
      }
    },
    {
      "id": "enum",
      "family": "Type/function names",
      "api": "Expr::cast_as_type qualified",
      "method": "GET",
      "route": "/case/protected/enum",
      "field": "input",
      "baseline": "ReviewStatus",
      "alternate": "ReviewStatus",
      "techniques": [
        "B",
        "E",
        "U",
        "S",
        "T",
        "Q"
      ],
      "controls": [
        "/case/control/enum"
      ],
      "fixed": {
        "table": "fixture.items",
        "tenant_guard": 1,
        "sentinel_id": 3
      },
      "inapplicable": {
        "Q": {
          "reason": "Inline-query tests are REPLACE-only (where=3), so the vector substitutes the whole parameter value with no prefix or suffix to escape quoting, and the technique confirms only by grepping its own delimiters out of the response body. The control puts the input in a double-quoted schema-qualified type name inside CAST, from which the vector cannot reach an unquoted, reflected projection; pgorm binds values and double-quotes identifiers, so no faithful control anywhere in this suite exposes one.",
          "evidence": {
            "kind": "no-boundary",
            "payload": "inline_query.xml",
            "where": [
              3
            ],
            "clause": [
              1,
              2,
              3,
              8
            ],
            "boundary": "boundaries.xml holds exactly one boundary whose <where> includes 3 (level 1, clause 0, ptype 1) and it carries an empty <prefix> and empty <suffix>; no where=3 boundary exists at any level that closes a ' or a \", so REPLACE can only land on an already-unquoted value.",
            "contexts": "4"
          }
        }
      }
    },
    {
      "id": "enum-ddl",
      "family": "Type/function names",
      "api": "ColumnDef::enumeration",
      "method": "GET",
      "route": "/case/protected/enum-ddl",
      "field": "input",
      "baseline": "ReviewStatus",
      "alternate": "ReviewStatus",
      "techniques": [
        "B",
        "E",
        "U",
        "S",
        "T",
        "Q"
      ],
      "controls": [
        "/case/control/enum-ddl"
      ],
      "fixed": {
        "table": "fixture.items",
        "tenant_guard": 1,
        "sentinel_id": 3
      },
      "inapplicable": {
        "Q": {
          "reason": "Inline-query tests are REPLACE-only (where=3), so the vector substitutes the whole parameter value with no prefix or suffix to escape quoting, and the technique confirms only by grepping its own delimiters out of the response body. The control puts the input in a double-quoted type name in a CREATE TABLE column definition, from which the vector cannot reach an unquoted, reflected projection; pgorm binds values and double-quotes identifiers, so no faithful control anywhere in this suite exposes one.",
          "evidence": {
            "kind": "no-boundary",
            "payload": "inline_query.xml",
            "where": [
              3
            ],
            "clause": [
              1,
              2,
              3,
              8
            ],
            "boundary": "boundaries.xml holds exactly one boundary whose <where> includes 3 (level 1, clause 0, ptype 1) and it carries an empty <prefix> and empty <suffix>; no where=3 boundary exists at any level that closes a ' or a \", so REPLACE can only land on an already-unquoted value.",
            "contexts": "4"
          }
        }
      }
    },
    {
      "id": "parameters",
      "family": "Parameter substitution",
      "api": "inject_parameters lexical contexts",
      "method": "GET",
      "route": "/case/protected/parameters",
      "field": "input",
      "baseline": "alice",
      "alternate": "bob",
      "techniques": [
        "B",
        "E",
        "U",
        "S",
        "T",
        "Q"
      ],
      "controls": [
        "/case/control/parameters"
      ],
      "fixed": {
        "table": "fixture.items",
        "tenant_guard": 1,
        "sentinel_id": 3
      },
      "inapplicable": {
        "Q": {
          "reason": "Inline-query tests are REPLACE-only (where=3), so the vector substitutes the whole parameter value with no prefix or suffix to escape quoting, and the technique confirms only by grepping its own delimiters out of the response body. The control puts the input in a single-quoted literal projected through ::text, from which the vector cannot reach an unquoted, reflected projection; pgorm binds values and double-quotes identifiers, so no faithful control anywhere in this suite exposes one.",
          "evidence": {
            "kind": "no-boundary",
            "payload": "inline_query.xml",
            "where": [
              3
            ],
            "clause": [
              1,
              2,
              3,
              8
            ],
            "boundary": "boundaries.xml holds exactly one boundary whose <where> includes 3 (level 1, clause 0, ptype 1) and it carries an empty <prefix> and empty <suffix>; no where=3 boundary exists at any level that closes a ' or a \", so REPLACE can only land on an already-unquoted value.",
            "contexts": "4"
          }
        }
      }
    },
    {
      "id": "contains",
      "family": "Pattern matching",
      "api": "ColumnTrait::contains",
      "method": "GET",
      "route": "/case/protected/contains",
      "field": "input",
      "baseline": "ali",
      "alternate": "bo",
      "techniques": [
        "B",
        "E",
        "U",
        "S",
        "T",
        "Q"
      ],
      "controls": [
        "/case/control/contains"
      ],
      "fixed": {
        "table": "fixture.items",
        "tenant_guard": 1,
        "sentinel_id": 3
      },
      "inapplicable": {
        "Q": {
          "reason": "Inline-query tests are REPLACE-only (where=3), so the vector substitutes the whole parameter value with no prefix or suffix to escape quoting, and the technique confirms only by grepping its own delimiters out of the response body. The control puts the input in a single-quoted LIKE pattern in a SELECT WHERE predicate, from which the vector cannot reach an unquoted, reflected projection; pgorm binds values and double-quotes identifiers, so no faithful control anywhere in this suite exposes one.",
          "evidence": {
            "kind": "no-boundary",
            "payload": "inline_query.xml",
            "where": [
              3
            ],
            "clause": [
              1,
              2,
              3,
              8
            ],
            "boundary": "boundaries.xml holds exactly one boundary whose <where> includes 3 (level 1, clause 0, ptype 1) and it carries an empty <prefix> and empty <suffix>; no where=3 boundary exists at any level that closes a ' or a \", so REPLACE can only land on an already-unquoted value.",
            "contexts": "4"
          }
        }
      }
    },
    {
      "id": "starts-with",
      "family": "Pattern matching",
      "api": "ColumnTrait::starts_with",
      "method": "GET",
      "route": "/case/protected/starts-with",
      "field": "input",
      "baseline": "ali",
      "alternate": "bo",
      "techniques": [
        "B",
        "E",
        "U",
        "S",
        "T",
        "Q"
      ],
      "controls": [
        "/case/control/starts-with"
      ],
      "fixed": {
        "table": "fixture.items",
        "tenant_guard": 1,
        "sentinel_id": 3
      },
      "inapplicable": {
        "Q": {
          "reason": "Inline-query tests are REPLACE-only (where=3), so the vector substitutes the whole parameter value with no prefix or suffix to escape quoting, and the technique confirms only by grepping its own delimiters out of the response body. The control puts the input in a single-quoted LIKE pattern in a SELECT WHERE predicate, from which the vector cannot reach an unquoted, reflected projection; pgorm binds values and double-quotes identifiers, so no faithful control anywhere in this suite exposes one.",
          "evidence": {
            "kind": "no-boundary",
            "payload": "inline_query.xml",
            "where": [
              3
            ],
            "clause": [
              1,
              2,
              3,
              8
            ],
            "boundary": "boundaries.xml holds exactly one boundary whose <where> includes 3 (level 1, clause 0, ptype 1) and it carries an empty <prefix> and empty <suffix>; no where=3 boundary exists at any level that closes a ' or a \", so REPLACE can only land on an already-unquoted value.",
            "contexts": "4"
          }
        }
      }
    },
    {
      "id": "ends-with",
      "family": "Pattern matching",
      "api": "ColumnTrait::ends_with",
      "method": "GET",
      "route": "/case/protected/ends-with",
      "field": "input",
      "baseline": "ice",
      "alternate": "ob",
      "techniques": [
        "B",
        "E",
        "U",
        "S",
        "T",
        "Q"
      ],
      "controls": [
        "/case/control/ends-with"
      ],
      "fixed": {
        "table": "fixture.items",
        "tenant_guard": 1,
        "sentinel_id": 3
      },
      "inapplicable": {
        "Q": {
          "reason": "Inline-query tests are REPLACE-only (where=3), so the vector substitutes the whole parameter value with no prefix or suffix to escape quoting, and the technique confirms only by grepping its own delimiters out of the response body. The control puts the input in a single-quoted LIKE pattern in a SELECT WHERE predicate, from which the vector cannot reach an unquoted, reflected projection; pgorm binds values and double-quotes identifiers, so no faithful control anywhere in this suite exposes one.",
          "evidence": {
            "kind": "no-boundary",
            "payload": "inline_query.xml",
            "where": [
              3
            ],
            "clause": [
              1,
              2,
              3,
              8
            ],
            "boundary": "boundaries.xml holds exactly one boundary whose <where> includes 3 (level 1, clause 0, ptype 1) and it carries an empty <prefix> and empty <suffix>; no where=3 boundary exists at any level that closes a ' or a \", so REPLACE can only land on an already-unquoted value.",
            "contexts": "4"
          }
        }
      }
    },
    {
      "id": "like",
      "family": "Pattern matching",
      "api": "ColumnTrait::like",
      "method": "GET",
      "route": "/case/protected/like",
      "field": "input",
      "baseline": "ali%",
      "alternate": "bo%",
      "techniques": [
        "B",
        "E",
        "U",
        "S",
        "T",
        "Q"
      ],
      "controls": [
        "/case/control/like"
      ],
      "fixed": {
        "table": "fixture.items",
        "tenant_guard": 1,
        "sentinel_id": 3
      },
      "inapplicable": {
        "Q": {
          "reason": "Inline-query tests are REPLACE-only (where=3), so the vector substitutes the whole parameter value with no prefix or suffix to escape quoting, and the technique confirms only by grepping its own delimiters out of the response body. The control puts the input in a single-quoted LIKE pattern in a SELECT WHERE predicate, from which the vector cannot reach an unquoted, reflected projection; pgorm binds values and double-quotes identifiers, so no faithful control anywhere in this suite exposes one.",
          "evidence": {
            "kind": "no-boundary",
            "payload": "inline_query.xml",
            "where": [
              3
            ],
            "clause": [
              1,
              2,
              3,
              8
            ],
            "boundary": "boundaries.xml holds exactly one boundary whose <where> includes 3 (level 1, clause 0, ptype 1) and it carries an empty <prefix> and empty <suffix>; no where=3 boundary exists at any level that closes a ' or a \", so REPLACE can only land on an already-unquoted value.",
            "contexts": "4"
          }
        }
      }
    },
    {
      "id": "direction",
      "family": "Typed structure",
      "api": "Order enum",
      "method": "GET",
      "route": "/case/protected/direction",
      "field": "input",
      "baseline": "asc",
      "alternate": "desc",
      "techniques": [
        "B",
        "E",
        "U",
        "S",
        "T",
        "Q"
      ],
      "controls": [
        "/case/control/direction"
      ],
      "fixed": {
        "table": "fixture.items",
        "tenant_guard": 1,
        "sentinel_id": 3
      },
      "inapplicable": {
        "Q": {
          "reason": "Inline-query tests are REPLACE-only (where=3), so the vector substitutes the whole parameter value with no prefix or suffix to escape quoting, and the technique confirms only by grepping its own delimiters out of the response body. The control puts the input in an unquoted ASC/DESC keyword after ORDER BY, from which the vector cannot reach an unquoted, reflected projection; pgorm binds values and double-quotes identifiers, so no faithful control anywhere in this suite exposes one.",
          "evidence": {
            "kind": "no-boundary",
            "payload": "inline_query.xml",
            "where": [
              3
            ],
            "clause": [
              1,
              2,
              3,
              8
            ],
            "boundary": "boundaries.xml holds exactly one boundary whose <where> includes 3 (level 1, clause 0, ptype 1) and it carries an empty <prefix> and empty <suffix>; no where=3 boundary exists at any level that closes a ' or a \", so REPLACE can only land on an already-unquoted value.",
            "contexts": "4"
          }
        },
        "U": {
          "reason": "UNION tests append at where=1 inside clause 1-5. The control puts the input in an unquoted ASC/DESC keyword after ORDER BY, and nothing may syntactically follow ORDER BY, LIMIT or OFFSET, so no UNION SELECT can attach while the injection point stays in that clause.",
          "evidence": {
            "kind": "no-attachable-position",
            "payload": "union_query.xml",
            "where": [
              1
            ],
            "clause": [
              1,
              2,
              3,
              4,
              5
            ],
            "boundary": "The ptype-1 and ptype-4 boundaries close the token, but a UNION SELECT appended after ORDER BY/LIMIT/OFFSET is a syntax error; no boundary relocates the injection point out of the terminal clause.",
            "contexts": "7a"
          }
        }
      }
    },
    {
      "id": "limit",
      "family": "Typed structure",
      "api": "SelectStatement::limit",
      "method": "GET",
      "route": "/case/protected/limit",
      "field": "input",
      "baseline": "1",
      "alternate": "2",
      "techniques": [
        "B",
        "E",
        "U",
        "S",
        "T",
        "Q"
      ],
      "controls": [
        "/case/control/limit"
      ],
      "fixed": {
        "table": "fixture.items",
        "tenant_guard": 1,
        "sentinel_id": 3
      },
      "inapplicable": {
        "Q": {
          "reason": "Inline-query tests are REPLACE-only (where=3), so the vector substitutes the whole parameter value with no prefix or suffix to escape quoting, and the technique confirms only by grepping its own delimiters out of the response body. The control puts the input in an unquoted integer closing a LIMIT clause, from which the vector cannot reach an unquoted, reflected projection; pgorm binds values and double-quotes identifiers, so no faithful control anywhere in this suite exposes one.",
          "evidence": {
            "kind": "no-boundary",
            "payload": "inline_query.xml",
            "where": [
              3
            ],
            "clause": [
              1,
              2,
              3,
              8
            ],
            "boundary": "boundaries.xml holds exactly one boundary whose <where> includes 3 (level 1, clause 0, ptype 1) and it carries an empty <prefix> and empty <suffix>; no where=3 boundary exists at any level that closes a ' or a \", so REPLACE can only land on an already-unquoted value.",
            "contexts": "4"
          }
        },
        "U": {
          "reason": "UNION tests append at where=1 inside clause 1-5. The control puts the input in an unquoted integer closing a LIMIT clause, and nothing may syntactically follow ORDER BY, LIMIT or OFFSET, so no UNION SELECT can attach while the injection point stays in that clause.",
          "evidence": {
            "kind": "no-attachable-position",
            "payload": "union_query.xml",
            "where": [
              1
            ],
            "clause": [
              1,
              2,
              3,
              4,
              5
            ],
            "boundary": "The ptype-1 and ptype-4 boundaries close the token, but a UNION SELECT appended after ORDER BY/LIMIT/OFFSET is a syntax error; no boundary relocates the injection point out of the terminal clause.",
            "contexts": "7a"
          }
        }
      }
    },
    {
      "id": "offset",
      "family": "Typed structure",
      "api": "SelectStatement::offset",
      "method": "GET",
      "route": "/case/protected/offset",
      "field": "input",
      "baseline": "1",
      "alternate": "2",
      "techniques": [
        "B",
        "E",
        "U",
        "S",
        "T",
        "Q"
      ],
      "controls": [
        "/case/control/offset"
      ],
      "fixed": {
        "table": "fixture.items",
        "tenant_guard": 1,
        "sentinel_id": 3
      },
      "inapplicable": {
        "Q": {
          "reason": "Inline-query tests are REPLACE-only (where=3), so the vector substitutes the whole parameter value with no prefix or suffix to escape quoting, and the technique confirms only by grepping its own delimiters out of the response body. The control puts the input in an unquoted integer closing an OFFSET clause, from which the vector cannot reach an unquoted, reflected projection; pgorm binds values and double-quotes identifiers, so no faithful control anywhere in this suite exposes one.",
          "evidence": {
            "kind": "no-boundary",
            "payload": "inline_query.xml",
            "where": [
              3
            ],
            "clause": [
              1,
              2,
              3,
              8
            ],
            "boundary": "boundaries.xml holds exactly one boundary whose <where> includes 3 (level 1, clause 0, ptype 1) and it carries an empty <prefix> and empty <suffix>; no where=3 boundary exists at any level that closes a ' or a \", so REPLACE can only land on an already-unquoted value.",
            "contexts": "4"
          }
        },
        "U": {
          "reason": "UNION tests append at where=1 inside clause 1-5. The control puts the input in an unquoted integer closing an OFFSET clause, and nothing may syntactically follow ORDER BY, LIMIT or OFFSET, so no UNION SELECT can attach while the injection point stays in that clause.",
          "evidence": {
            "kind": "no-attachable-position",
            "payload": "union_query.xml",
            "where": [
              1
            ],
            "clause": [
              1,
              2,
              3,
              4,
              5
            ],
            "boundary": "The ptype-1 and ptype-4 boundaries close the token, but a UNION SELECT appended after ORDER BY/LIMIT/OFFSET is a syntax error; no boundary relocates the injection point out of the terminal clause.",
            "contexts": "7a"
          }
        }
      }
    },
    {
      "id": "stored-value",
      "family": "Stored inputs",
      "api": "stored value -> Entity filter",
      "method": "GET",
      "route": "/case/protected/stored-value",
      "field": "input",
      "baseline": "alice",
      "alternate": "bob",
      "techniques": [
        "B",
        "E",
        "U",
        "S",
        "T",
        "Q"
      ],
      "controls": [
        "/case/control/stored-value"
      ],
      "fixed": {
        "table": "fixture.items",
        "tenant_guard": 1,
        "sentinel_id": 3
      },
      "inapplicable": {
        "Q": {
          "reason": "Inline-query tests are REPLACE-only (where=3), so the vector substitutes the whole parameter value with no prefix or suffix to escape quoting, and the technique confirms only by grepping its own delimiters out of the response body. The control puts the input in a single-quoted literal in a SELECT WHERE predicate reached through stored state, from which the vector cannot reach an unquoted, reflected projection; pgorm binds values and double-quotes identifiers, so no faithful control anywhere in this suite exposes one.",
          "evidence": {
            "kind": "no-boundary",
            "payload": "inline_query.xml",
            "where": [
              3
            ],
            "clause": [
              1,
              2,
              3,
              8
            ],
            "boundary": "boundaries.xml holds exactly one boundary whose <where> includes 3 (level 1, clause 0, ptype 1) and it carries an empty <prefix> and empty <suffix>; no where=3 boundary exists at any level that closes a ' or a \", so REPLACE can only land on an already-unquoted value.",
            "contexts": "4"
          }
        }
      }
    },
    {
      "id": "stored-identifier",
      "family": "Stored inputs",
      "api": "stored value -> column",
      "method": "GET",
      "route": "/case/protected/stored-identifier",
      "field": "input",
      "baseline": "name",
      "alternate": "note",
      "techniques": [
        "B",
        "E",
        "U",
        "S",
        "T",
        "Q"
      ],
      "controls": [
        "/case/control/stored-identifier"
      ],
      "fixed": {
        "table": "fixture.items",
        "tenant_guard": 1,
        "sentinel_id": 3
      },
      "inapplicable": {
        "E": {
          "reason": "The control puts the input in a double-quoted leading projected identifier, a projected column that depends on the trailing `FROM items`. Attaching an error-forcing subquery fails because appending an error subquery makes `SELECT (text AND bool)` a type error. The only faithful repair moves the identifier to a trailing slot, which changes what the case tests.",
          "evidence": {
            "kind": "no-attachable-position",
            "payload": "error_based.xml",
            "where": [
              1,
              3
            ],
            "clause": [
              1,
              2,
              3,
              8,
              9
            ],
            "boundary": "Every level-3 boundary either comments forward, deleting the `FROM items` the projected name resolves against, or appends a predicate, making `SELECT (text AND bool)` a type error; the clause-8 identifier boundaries are level 4-5 and rescue no technique but B.",
            "contexts": "7a"
          }
        },
        "Q": {
          "reason": "Inline-query tests are REPLACE-only (where=3), so the vector substitutes the whole parameter value with no prefix or suffix to escape quoting, and the technique confirms only by grepping its own delimiters out of the response body. The control puts the input in a double-quoted leading projected identifier, from which the vector cannot reach an unquoted, reflected projection; pgorm binds values and double-quotes identifiers, so no faithful control anywhere in this suite exposes one.",
          "evidence": {
            "kind": "no-boundary",
            "payload": "inline_query.xml",
            "where": [
              3
            ],
            "clause": [
              1,
              2,
              3,
              8
            ],
            "boundary": "boundaries.xml holds exactly one boundary whose <where> includes 3 (level 1, clause 0, ptype 1) and it carries an empty <prefix> and empty <suffix>; no where=3 boundary exists at any level that closes a ' or a \", so REPLACE can only land on an already-unquoted value.",
            "contexts": "4"
          }
        },
        "S": {
          "reason": "The control puts the input in a double-quoted leading projected identifier, a projected column that depends on the trailing `FROM items`. Attaching a `;<statement>` batch continuation fails because the first statement of the batch loses its `FROM items` to the payload's own comment, so the batch errors before the appended statement runs. The only faithful repair moves the identifier to a trailing slot, which changes what the case tests.",
          "evidence": {
            "kind": "no-attachable-position",
            "payload": "stacked_queries.xml",
            "where": [
              1
            ],
            "clause": [
              1,
              2,
              3,
              4,
              5,
              6,
              7,
              8
            ],
            "boundary": "Every level-3 boundary either comments forward, deleting the `FROM items` the projected name resolves against, or appends a predicate, making `SELECT (text AND bool)` a type error; the clause-8 identifier boundaries are level 4-5 and rescue no technique but B.",
            "contexts": "7a"
          }
        },
        "T": {
          "reason": "The control puts the input in a double-quoted leading projected identifier, a projected column that depends on the trailing `FROM items`. Attaching a timing subquery fails because appending a timing subquery makes `SELECT (text AND bool)` a type error. The only faithful repair moves the identifier to a trailing slot, which changes what the case tests.",
          "evidence": {
            "kind": "no-attachable-position",
            "payload": "time_blind.xml",
            "where": [
              1,
              3
            ],
            "clause": [
              1,
              2,
              3,
              8,
              9
            ],
            "boundary": "Every level-3 boundary either comments forward, deleting the `FROM items` the projected name resolves against, or appends a predicate, making `SELECT (text AND bool)` a type error; the clause-8 identifier boundaries are level 4-5 and rescue no technique but B.",
            "contexts": "7a"
          }
        },
        "U": {
          "reason": "The control puts the input in a double-quoted leading projected identifier, a projected column that depends on the trailing `FROM items`. Attaching a `UNION SELECT` set operation fails because commenting forward deletes the `FROM items` the UNION's left statement needs. The only faithful repair moves the identifier to a trailing slot, which changes what the case tests.",
          "evidence": {
            "kind": "no-attachable-position",
            "payload": "union_query.xml",
            "where": [
              1
            ],
            "clause": [
              1,
              2,
              3,
              4,
              5
            ],
            "boundary": "Every level-3 boundary either comments forward, deleting the `FROM items` the projected name resolves against, or appends a predicate, making `SELECT (text AND bool)` a type error; the clause-8 identifier boundaries are level 4-5 and rescue no technique but B.",
            "contexts": "7a"
          }
        }
      }
    }
  ]
}
