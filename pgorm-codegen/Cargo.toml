[package]
name = "pgorm-codegen"
version = "0.2.0"
authors = ["Billy Chan <ccw.billy.123@gmail.com>"]
edition = "2024"
description = "Code Generator for pgorm"
license = "MIT OR Apache-2.0"
homepage = "https://github.com/pgorm-rs/pgorm"
documentation = "https://docs.rs/pgorm-codegen"
repository = "https://github.com/pgorm-rs/pgorm"
categories = ["database"]
keywords = ["sql", "postgres", "codegen"]
rust-version = "1.88"

[lib]
name = "pgorm_codegen"
path = "src/lib.rs"

[dependencies]
pgorm-query.workspace = true
# libpg_query, the PostgreSQL server's own parser, is a plain dependency here:
# pgorm-codegen is a build-time tool nothing links into a running application,
# so the C build cost lands on people generating entities and on nobody else.
pg_query = "6.2.0"
syn = { version = "3", default-features = false, features = ["parsing", "proc-macro", "derive", "printing"] }
quote = { version = "1.0.47", default-features = false }
heck = { version = "0.5", default-features = false }
proc-macro2 = { version = "1.0.107", default-features = false }
tracing = { version = "0.1.44", default-features = false, features = ["log"] }

[dev-dependencies]
pretty_assertions = { version = "1.4" }
pgorm = { path = "../", default-features = false, features = ["macros"] }
